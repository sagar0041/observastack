spring:
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/observastack
    username: observastack
    password: observastack
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8081

inventory:
  base-url: http://localhost:8082
