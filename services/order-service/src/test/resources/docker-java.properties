# Pins the Docker Engine API version docker-java negotiates with, rather
# than letting it auto-detect. Needed in this environment because the
# daemon's minimum supported API version (1.40) is newer than what some
# docker-java releases probe with by default. 1.44 is comfortably within
# range for any reasonably current Docker Engine.
api.version=1.44
