# Services

Spring Boot modules get added here one at a time, per PLAN.md:
- order-service       (P1)
- inventory-service   (P2)
- payment-service     (P7, added when the Kafka async flow is built)

Each service should be independently runnable via `docker compose up`.
