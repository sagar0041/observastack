# Incident Runbook

TODO (P9): after the deliberate fault-injection exercise, document:
- what broke and why
- which signal caught it first (metric, trace, or log)
- the exact query/dashboard that pinpointed root cause
- time-to-diagnosis
- the fix

This becomes the single most useful page in the repo for interviews —
write it like an honest postmortem, not a highlight reel.
