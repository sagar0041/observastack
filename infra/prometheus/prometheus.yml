global:
  scrape_interval: 5s
scrape_configs:
  # TODO (P5): add scrape targets for order-service, inventory-service,
  # payment-service once their /actuator/prometheus endpoints exist.
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
