apiVersion: 1
providers:
  - name: ObservaStack
    folder: ObservaStack
    type: file
    options:
      path: /var/lib/grafana/dashboards
# Dashboard JSON files get added here in P6 (checked into git, not
# clicked together in the UI, so they're reviewable and reproducible).
